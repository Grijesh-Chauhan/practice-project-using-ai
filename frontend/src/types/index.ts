/** Shared TypeScript types (aligned with API contract in later phases). */

export type TicketStatus = "Open" | "In Progress" | "Resolved" | "Closed" | "Blocked";

export type TicketPriority = "Low" | "Medium" | "High" | "Critical";
